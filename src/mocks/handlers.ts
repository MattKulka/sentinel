import type { RequestHandler } from 'msw';

// Fleshed out in Milestone 1 (Task 1.1). Kept as an empty array during scaffold
// so the Vitest setup file can wire up the MSW server.
export const handlers: RequestHandler[] = [];
